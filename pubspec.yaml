name: my_flutter_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  video_player: ^2.6.3
  fl_chart: ^0.70.1 # Added missing `fl_chart` dependency
  intl: ^0.18.0
  image_picker: ^0.8.7+3
  camera: ^0.10.0+2
  google_ml_kit: ^0.19.0
  http: ^0.13.6

dev_dependencies:
  flutter_test:
  
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/videos/boys.mp4
    - assets/videos/boys1.mp4
    - assets/videos/boys2.mp4

